# bin/

This folder is intentionally empty in the repository.

The compiled `windows-fps-boost-utility.exe` is published on the
[Releases page](../../releases) — download the latest version from there.

Do not commit `.exe` files to the repository:
- they bloat git history permanently
- GitHub rejects files > 100 MB
- antivirus false positives are common with committed binaries